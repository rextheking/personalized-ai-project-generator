// Copy this file to config.js, or let `make upload-web` generate it at deploy
// time. It sets the API endpoint the frontend calls. config.js is gitignored
// so your live endpoint never gets committed.
window.API_ENDPOINT = "REPLACE_WITH_YOUR_API_ENDPOINT";
